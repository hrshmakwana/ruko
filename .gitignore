# --- secrets: never commit ---
.env
.env.*
!.env.example
*.pem
*.key
credentials
.aws/

# --- Harsh's real samples: private, never committed ---
samples/private/

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# --- sam / build artifacts ---
.aws-sam/
backend/.aws-sam/
packaged.yaml
*.zip

# --- node / vite ---
node_modules/
dist/
dist-ssr/
.vite/
*.local

# --- editors / os ---
.DS_Store
.idea/
.vscode/
*.swp

# python venv used for running the backend tests locally
backend/.venv/

# relative-base preview build, published for phone testing
frontend/dist-preview/

# never again: a key pasted into a document in the repo
*api*key*
*.pdf
